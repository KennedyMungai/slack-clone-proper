type Props = {
  count?: number;
  image?: string;
  timestamp?: number;
  onClick?: () => void;
};

const ThreadBar = ({ count, timestamp, image }: Props) => {
  return <div>ThreadBar</div>;
};

export default ThreadBar;
